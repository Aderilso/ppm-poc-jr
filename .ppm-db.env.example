# PPM local database configuration (SQLite via Prisma)
# Copy this file to `.ppm-db.env` and adjust as needed.

# Example: use a project-local DB file
# DATABASE_URL=file:${PWD}/server/prisma/dev.local.db

# Recommended: use user home data folder (works across OSes)
# macOS/Linux
# DATABASE_URL=file:${HOME}/.ppm-data/dev.db
# Windows (PowerShell expands %USERPROFILE%)
# DATABASE_URL=file:%USERPROFILE%\.ppm-data\dev.db

